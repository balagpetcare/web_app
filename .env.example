# Best practice (recommended): call the API via same-origin proxy (Next.js rewrite).
# Leave this empty so the browser calls relative URLs like /api/v1/...
NEXT_PUBLIC_API_BASE_URL=""

# Where the Next.js rewrite proxy should forward /api/* requests.
# This points to your Node.js backend.
API_BASE_URL="http://localhost:3000"
