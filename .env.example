# BPA Admin UI (multi-panel)
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000

# Central Auth Server URL (for login/register redirects)
# This is where users are redirected for authentication
NEXT_PUBLIC_AUTH_BASE_URL=http://localhost:3000

# Cookie name set by your Node.js API for admin auth (HttpOnly cookie)
AUTH_COOKIE_NAME=bpa_admin

# Optional: show which panel is default when opening /
NEXT_PUBLIC_DEFAULT_PANEL=admin
